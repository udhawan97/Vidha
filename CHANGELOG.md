# Changelog

All notable changes are documented here. Vidha has not published an application version.

## Unreleased

- Renamed the provisional working product from `Viraha` to `Vidha`; no release or shipped application exists.
- Established the Vidha product vocabulary and scope.
- Recorded initial architecture, licensing, encryption, and Release Policy decisions.
- Added the pre-v1 product, security, public-surface, and release documentation.
- Added a TypeScript monorepo with a pure, deterministic Check-in timeline that deliberately stops at Concern.
- Added a responsive React/Vite PWA prototype with synthetic data, temporary text editing and import/export, and a prompted update flow.
- Added an in-app Owner guide and bounded multi-file Attachment-candidate rehearsal across common document, image, audio, video, data, contact, and ZIP formats while keeping upload, scanning, encryption, persistence, delivery, and Release explicitly absent.
- Added domain, component, WebKit desktop/mobile, accessibility, PWA, build, lint, formatting, and documentation checks.
- Expanded the synthetic document rehearsal with Recipient reassignment, undo and redo, bounded document-only session versions, reviewed safe restore that preserves the current draft, browser-decoded text snapshots for import restore, and Markdown, plain-text, and escaped standalone HTML copies.
- Implemented the next six bounded foundation slices with synthetic identities and disposable state: a provider-neutral authenticated-principal/session input boundary, application authorization, atomic content-free audit/idempotency storage, memory/SQLite/PGlite parity, explicit Plan lifecycle, and quarantined text intake.
- Hardened those slices so authorization and recent authentication precede replay handling, caller idempotency values never enter domain or persisted exports, semantic key conflicts fail closed, timeline numbers remain portable safe integers, PGlite snapshots are transactionally consistent, and import approval is bound to intake-owned scanned bytes.
- Preserved exact approved import bytes only for the browser session, with explicit warnings that the fixture inspection is not malware scanning or sandboxed conversion.
- Kept real accounts, durable personal data, Guardian authority, production cryptography and providers, notifications, and Release outside the implemented claim; the production database direction remained deferred at the end of Phase 2.
- Added a Phase 3 Owner identity coordinator that generates canonical synthetic sessions, retains only session digests, rejects global token collisions, requires proof-adapter UP/UV, serializes revisioned and semantically idempotent mutations, models retry-safe credential/session revocation, requires two independently modeled recovery proofs plus cooling-off, verifies and permits cancellation of new channel references, and emits content-free notice intent.
- Changed the application boundary to resolve Owner, authentication, and expiry only through `SessionVerifier`; forged caller principal/time fields are ignored and unknown sessions fail before a transaction.
- Selected the self-hostable target of one API/worker application image with PostgreSQL and replaceable adapters, while keeping PGlite as disposable contract evidence rather than a production-database claim.
- Added bounded encrypted operational metadata with an AES-256-GCM fixture, authenticated record/schema context, IV-reuse failure, retention/deletion, ciphertext-only snapshots, and restore-safe reads; production key custody, wrapped keys, authenticated backups, and Standard/Sealed Mode remain absent.
- Added durable scheduled-job and synthetic-outbox contracts with atomic encrypted-state/outbox commit, positive task allowlists, semantic idempotency, bounded retries, dead letter, expiring leases, monotonic fencing, stale-worker rejection, and memory/PGlite parity.
- Bound import inspection evidence to exact digest and byte count, engine/signature versions, duration, verdict, and an accepted isolation profile. The browser adapter remains explicitly `synthetic_fixture`; the later Phase 3B executable gate does not change that browser claim.
- Recorded primary-source decisions and ADRs for WebAuthn/revocable sessions, recovery, PostgreSQL topology, encrypted operations, fenced work, and isolated import processing, then bounded the next six executable-evidence slices in the Phase 3B completion plan.
- Added the Phase 3B disposable PostgreSQL 18 platform: checksum-locked migrations, runtime configuration matching, a one-shot migrator, least-privilege API/worker roles, durable Owner identity/recovery/WebAuthn state, Plan/audit persistence, database-time leases, fencing, restore-safe denial, and an idempotent content-free sink.
- Pinned `@simplewebauthn/server` and implemented exact RP/origin, one-time bootstrap, registration/authentication/reauthentication, UP/UV, counter, expiring challenge, and one-time assertion-proof adapters without enabling a browser endpoint or real enrollment.
- Added per-record wrapped AES-GCM metadata keys, AES-KW rewrap, fail-closed key loss/tamper handling, Ed25519-signed backup generation chains, and an external anti-rollback inventory fixture; external key custody and database restore remain unproved.
- Made accepted PostgreSQL Plan state, processed-command history, append-only audit, next-stage schedule, and content-free synthetic outbox intent commit atomically, including rollback on semantic conflict and concurrent duplicate handling.
- Added source-hash-pinned file 5.48, digest-pinned ClamAV 1.5.4 `INSTREAM`, and Pandoc 3.10.2 JSON conversion gates with bounded output, node/link allowlists, cleanup, benign/EICAR fixtures, and explicit host-process rather than rootless-sandbox boundaries.
- Added closure-slice-6 rootless Podman execution for file/Pandoc jobs, immutable ClamAV signature-manifest identity, a checked SPDX/license inventory, read-only/no-network/resource/credential/cleanup controls, and generated polyglot, high-ratio archive, SSRF, timeout, output-flood, malformed-AST, and symlink fixtures. This remains a disposable runner result, not a real upload or general sandbox claim.
- Kept Phase 3B explicitly in progress and planned its six remaining closure slices: topology failures/capacity, authenticated database restore and atomic key rotation, browser WebAuthn/cookies, wired recovery abuse, integrated scheduled-command crashes, and rootless adversarial import isolation with separate signature-update authority.
- Implemented the Phase 3B closure-slice-1 source gate: identity-checked PostgreSQL backend termination at six migration and four claim checkpoints, pre-commit rollback, post-commit lost-ack replay/fencing, one worker-network disconnect/reconnect, bounded pool exhaustion, 1,000 due jobs amid 100,000 audit and 100,000 outbox rows, run-owned cleanup, Compose teardown, and tmpfs data-root destruction. Exact-commit acceptance remains mandatory; this is not production sizing, general outage recovery, failover, durable infrastructure, deployment, or release authority.
- Implemented the Phase 3B closure-slice-2 source gate: atomic persisted metadata-key rewrap and immutable replay reports; wrapped-key authenticated PostgreSQL custom archives; signed generations whose external file inventory advances only after artifact persistence; exact non-current retention retirement; truncated/tampered/rollback/key-loss rejection; a dedicated non-superuser logical restore; restore-safe portable-state and migration invariants; read-only promotion denial; and immutable explicit promotion. Exact-commit CI acceptance remains mandatory; the fixtures do not establish external key custody, streaming or durable backup, persistent-volume recovery, RPO/RTO, Standard Mode, deployment, or release authority.
- Implemented the Phase 3B closure-slice-3 source gate: an opt-in `127.0.0.1` WebAuthn rehearsal with an exact localhost RP/origin, synthetic one-time bootstrap, standards-bounded user handles, bounded same-origin JSON routes, digest-backed Secure/HttpOnly/SameSite=Strict `__Host-` cookies, server-derived CSRF, atomic session rotation, replay denial, revocation, and expiry. Chromium exercises the full virtual-authenticator ceremony; Firefox and WebKit exercise only the HTTP/session boundary. Exact-commit CI acceptance remains mandatory, and this is not real-authenticator, Safari, supported-browser, public-origin, production-session, deployment, or release evidence.
- Implemented the Phase 3B closure-slice-4 source gate: recovery proof acceptance, consumption, and cancellation now occur only through serialized identity commands; separately issued/stored domain-separated factor digests, replay-safe concurrent issuance, durable per-factor and pair failure counters, Owner-wide escalating locks across attempts, concurrent completion/cancellation, prior-session invalidation, schema migration splitting/tombstones, cross-linked authenticated backup/restore inspection, restore-safe denial, post-promotion cancellation, narrowed database grants, and content-free notices are covered with synthetic fixtures. The loopback rehearsal now reloads credential/session state, fails closed if hydration cannot establish authority, and gives a concrete lost-credential stop path while exposing no recovery completion, email/SMS fallback, Verified Owner Channel delivery, real factor, real destination, deployment, or release authority.
- Implemented the Phase 3B closure-slice-5 source gate: the runtime worker now passes only opaque claim/fencing identifiers to a PostgreSQL adapter that derives database time and canonical policy-revision-bound `ADVANCE_TIME`; accepted Plan state, command history, audit, next-stage work, and current-job completion commit atomically. The disposable matrix terminates four identity-checked execution backends, advances one historical stage per command through Concern, rejects stale schedules, exhausts retries into inspectable dead letters, replays the synthetic sink after acceptance, and emits allowlisted content-free telemetry. Exact-commit CI acceptance remains mandatory; no provider, real destination, Guardian authority, state beyond Concern, deployment, or release is enabled.
- Improved the synthetic Owner flow by putting the current lifecycle action before unavailable timeline controls on Draft, Paused, and Disabled mobile states and by opening a selected demo Envelope directly from its Overview review action.
- Serialized synthetic Owner actions so repeated activation cannot submit a second semantic command, replaced Check-in and Disable overlays with safe-default native dialogs that contain and return focus, kept failed confirmations open with an honest error, and added a confirmed fresh-session action that clears every local edit, Attachment, Document Version, review, event, and runtime before loading a separate Draft without resuming the Disabled Plan.
- Added Phase 3G session-aware reload protection and an explicit service-worker update decision: accepted browser work activates `beforeunload`, updates wait for Owner actions, the safe default preserves the rehearsal, the destructive choice names every in-memory surface it clears, and update failure restores protection without claiming persistence, migration, installed-app acceptance, or bad-service-worker recovery.

No application version has been released.
