# Phase 3B closure six

**Status:** In progress. Closure slice 1 is implemented in source behind a mandatory disposable CI rehearsal; exact-commit acceptance requires that gate to pass. Slices 2–6 remain planned. Phase 3B is not complete, deployed, production-ready, or release authority.

These slices close the executable-evidence gaps that remain after the intermediate Phase 3B source milestone. They continue to use disposable synthetic identities, content, channels, and destinations and stop at Concern.

| Order | Bounded slice                                    | Status                          | Intended outcome                                                                                                  | Required evidence and stop condition                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ------------------------------------------------ | ------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | Topology failure and capacity rehearsal          | Source implemented; CI required | Prove that the pinned migrator/API/worker/PostgreSQL topology behaves safely across interruption and bounded load | PostgreSQL backend termination at all six defined migration checkpoints and all four defined claim checkpoints, including post-commit lost acknowledgements; fenced restart before/after sink acceptance; one worker-network partition/recovery probe; pool exhaustion/recovery; 1,000 due jobs amid 100,000 outbox and 100,000 audit rows; run-owned cleanup; Compose teardown; and fresh-root proof |
| 2     | Authenticated database backup and key rotation   | Planned                         | Connect wrapped key custody to a real logical backup, atomic rotation, and isolated restore-safe database         | Pinned `pg_dump`/`pg_restore`, age- or KMS-shaped encryption adapter, signed generation chain, external inventory, atomic persisted key-version rotation, interrupted rotation/backup, tamper/rollback/key-loss cases, least-privilege restore, invariant report, explicit promotion, retention inventory, and deletion boundaries. Stop before personal content, RPO/RTO, or Standard Mode claims    |
| 3     | Browser WebAuthn and session boundary            | Planned                         | Wire the ceremony adapter to a disposable local API and opaque secure cookie without broadening product state     | Chromium virtual authenticator plus Firefox and target-WebKit acceptance where supported; exact RP/origin, bootstrap concurrency, replay, UP/UV, counter, CSRF, SameSite/Secure/HttpOnly, rotation, revocation, and expiry tests. Stop before a real authenticator, person, or public endpoint                                                                                                        |
| 4     | Durable recovery abuse matrix                    | Planned                         | Wire recovery through identity commands and prove cancellation, rate limiting, and revision behavior              | Concurrent completion/cancellation, multi-attempt lock escalation, independent proof compromise, session invalidation, channel-change cooling-off, migration/restore, digest-only inspection, and content-free notices. Keep real recovery completion and channel delivery disabled                                                                                                                   |
| 5     | Integrated scheduled command crash matrix        | Planned                         | Let the worker submit only canonical `ADVANCE_TIME` commands and atomically settle/reschedule work                | One-stage downtime catch-up, state/audit/job settlement in one transaction, crashes before/after decision and sink acceptance, stale policy revision, disconnect, retry exhaustion, dead-letter inspection, and redacted telemetry. Stop at Concern and keep the positive task allowlist unchanged                                                                                                    |
| 6     | Rootless import isolation and adversarial corpus | Planned                         | Separate signature update authority from runtime scanning and run tools in a bounded rootless boundary            | A separately identified updater and immutable runtime signature-set identity; pinned image/SBOM/license inventory; mismatch, polyglot, archive bomb, SSRF, timeout, output flood, malformed AST, symlink, credential/network denial, cleanup, and content-free log evidence. Stop before a real upload or general sandbox-support claim                                                               |

Slice 1 uses only a run-sentinel-owned PostgreSQL database, run-unique content-free job/audit rows, a run-unique digest-pinned Compose fixture, and a tmpfs database root. Its migration and claim probes terminate identity-checked PostgreSQL backends, distinguish pre-commit rollback from post-commit lost acknowledgement, and retry with checksum/idempotency and fencing checks. Its worker-partition fixture uses a five-second client query-read timeout so a severed half-open socket reaches the bounded recovery path; that fixture value is not a production latency policy. The gate drops only its sentinel-owned rehearsal database, deletes only its generated capacity rows, removes its Compose project, restarts PostgreSQL without the application schema, and tears it down again. A passing exact-commit gate proves this bounded disposable behavior; it is not failover, high availability, general outage recovery, a production capacity claim, or persistent-volume evidence.

## Gate after Phase 3B

Even if all six pass, Vidha still has no authority to accept real data, contact real people, add Guardian Attestations, implement final notice, Veto Window, Delivery Hold, Automatic Fallback, or Release, deploy publicly, publish a tag, create a GitHub release, or call itself v1. The threat model and sections 1–11 of `docs/release/V1_RELEASE_GATES.md` remain controlling.
