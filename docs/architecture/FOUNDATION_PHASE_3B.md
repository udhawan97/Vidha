# Phase 3B executable foundation

**Status:** Phase 3B is in progress. The six intermediate foundation slices and closure slices 1–5 now have disposable, synthetic executable gates; every exact commit still requires those gates to pass. This is not a completed phase, hosted deployment, production environment, real-account system, Standard Mode, Sealed Mode, Release authority, or v1 release.

Phase 3B moves the accepted Phase 3 operational decisions into production-shaped adapters while preserving the hard stop at Concern. The runnable browser prototype remains in-memory and synthetic; the new server adapters are exercised only by local unit contracts and mandatory disposable CI gates.

## Six implemented foundation slices

| Slice                                                 | Executable evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Boundary that remains                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PostgreSQL topology                                   | Digest-pinned Node and PostgreSQL 18 images; one application image; migrator/API/worker roles; loopback API edge; internal data network; non-root runtime; checksum-locked migrations; backend termination at six defined migration and four defined claim checkpoints; lost-ack replay and fencing; one partition/recovery probe; two-connection pool exhaustion; 1,000 due jobs with 100,000 audit and 100,000 outbox rows; run-owned teardown and fresh-root checks                                       | No public DNS, production secret, durable volume, cross-host failover, high availability, general outage recovery, production capacity result, or deployment exists                                                                                                                                                                |
| Wrapped-key metadata and authenticated logical backup | Per-record AES-GCM data keys wrapped by an injected AES-KW provider; atomic persisted rewrap without ciphertext change; immutable retry-safe rotation reports; fail-closed interruption/missing/wrong-key tests; Ed25519-signed generation/parent manifests; external atomic file inventory and non-current retention retirement; digest-pinned PostgreSQL 18 custom dump/restore; dedicated non-superuser restore, restore-safe invariant inspection, read-only denial, and explicit promotion rehearsal    | Key and signer providers remain in-memory WebCrypto fixtures, and archive encryption is bounded in memory. There is no external KMS custody, durable backup destination, streaming archive encryption, production retention destruction, persistent-volume evidence, RPO/RTO, or Standard Mode claim                               |
| WebAuthn ceremonies and session boundary              | Pinned `@simplewebauthn/server`; exact RP/origin configuration; loopback-only one-time bootstrap; registration/authentication/reauthentication ceremony purposes; expiring one-time challenges and assertion proofs; required user presence and verification; durable counters; standards-bounded user handles; optional exact-origin HTTP routes; opaque digest-backed `__Host-` cookies; session-bound CSRF; atomic rotation, revocation, and expiry                                                       | The route is disabled by default and can bind only to loopback. Chromium supplies virtual-authenticator ceremony evidence; Firefox/WebKit cover only the HTTP/session boundary. There is no real authenticator/person, Safari or supported-browser result, identity proofing, TLS/public origin, production session, or deployment |
| Durable identity and recovery                         | PostgreSQL Owner Identity repository, revision serialization, digest-only session persistence, command replay protection, atomic ceremony/proof consumption, separately issued/stored domain-separated recovery factors, transactional accept/consume/cancel, concurrent completion/cancellation serialization, per-factor counters, Owner-wide escalating abuse locks, session invalidation, migration tombstones, restore-safe inspection, post-promotion cancellation, and content-free notice intents    | Recovery completion remains synthetic and disconnected from any real channel. The loopback rehearsal exposes no recovery command or channel delivery. No real account, recovery factor/provider/destination, identity proofing, or production session exists                                                                       |
| PostgreSQL Plan scheduler/outbox                      | PostgreSQL Plan state, processed-command, append-only audit, next-stage allowed work, and claimed-job settlement share one transaction; the worker supplies only opaque claim/fencing identifiers; the adapter derives database time and canonical policy-revision-bound `ADVANCE_TIME` semantics; four execution crash boundaries, one-stage downtime catch-up through Concern, stale-schedule rejection, bounded retry/dead letter, redacted telemetry, and idempotent synthetic-sink replay are exercised | Exact-commit disposable PostgreSQL and topology acceptance remains required. There is no notification provider and the schema contains no Guardian, Recipient, final-notice, Veto Window, Delivery Hold, Automatic Fallback, or Release task                                                                                       |
| Executable import pipeline                            | Exact-byte libmagic classification, ClamAV `INSTREAM`, Pandoc sandbox-mode JSON conversion, node/link allowlists, size/time/output bounds, cleanup, benign Markdown, EICAR, mismatch, malformed output, and protocol framing tests; source and image versions are pinned                                                                                                                                                                                                                                     | The mandatory executable gate currently invokes file and Pandoc as host CI processes and ClamAV in a container. It is not rootless OCI isolation evidence, an adversarial corpus, archive handling, a real upload, or a sandbox-support claim                                                                                      |

## Verification boundary

`pnpm check` is the local deterministic gate. It includes format, lint, type, unit/contract, build, and documentation checks. The mandatory GitHub workflow adds:

- a fresh PostgreSQL 18 service and cross-process persistence/contention/restore-safe tests;
- PostgreSQL migration/claim backend termination, rollback/restart, pool exhaustion, and the fixed synthetic capacity profile;
- the digest-pinned migrator/API/worker/PostgreSQL Compose topology, including worker partition/recovery and tmpfs-root destruction;
- the authenticated logical-backup, atomic key-rotation, external inventory, isolated restore-safe inspection, and explicit-promotion rehearsal;
- the opt-in loopback WebAuthn/session rehearsal: Chromium virtual-authenticator ceremonies plus Firefox/WebKit same-origin HTTP, header, cookie, CSRF, rotation, revocation, replay, and expiry boundaries;
- the PostgreSQL recovery abuse matrix: command-only proof transitions, independent-factor compromise, multi-attempt lock escalation, concurrent completion/cancellation, migration and authenticated-restore invariants, session invalidation, and content-free notice shape;
- the integrated scheduled-command matrix: four identity-checked execution terminations, atomic state/audit/outbox/job settlement, one-stage historical catch-up, stale policy-revision rejection, retry exhaustion, dead-letter inspection, synthetic-sink replay, and content-free telemetry;
- source-hash-verified file 5.48, Pandoc 3.10.2, and digest-pinned ClamAV 1.5.4 executable tests; and
- desktop and mobile WebKit acceptance for the existing synthetic PWA.

Passing that workflow verifies only the disposable fixtures at the tested commit. It does not pass sections 1–11 of the v1 release gates.

## Stop condition

After the exact-commit slice-5 CI gate passes, the next work begins with closure slice 6 in the [Phase 3B closure six](../product/PHASE_3B_CLOSURE_SIX.md). Do not accept personal data, enroll a real identity, contact a real destination, expose the runtime publicly, add Guardian authority, implement any state beyond Concern, publish a tag, create a GitHub release, or call this v1.
