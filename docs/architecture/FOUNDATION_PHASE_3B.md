# Phase 3B executable foundation

**Status:** Phase 3B is in progress. This document records an intermediate disposable, synthetic source milestone—not a completed phase, hosted deployment, production environment, real-account system, Standard Mode, Sealed Mode, Release authority, or v1 release.

Phase 3B moves the accepted Phase 3 operational decisions into production-shaped adapters while preserving the hard stop at Concern. The runnable browser prototype remains in-memory and synthetic; the new server adapters are exercised only by local unit contracts and mandatory disposable CI gates.

## Six implemented foundation slices

| Slice                                     | Executable evidence                                                                                                                                                                                                                                                                                                                                                                | Boundary that remains                                                                                                                                                                                                                                                        |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PostgreSQL topology                       | Digest-pinned Node and PostgreSQL 18 images; one bundled application image; separate one-shot migrator, API, and worker roles; loopback-only API port; internal network; non-root runtime; checksum-locked migrations; runtime configuration matching; health/readiness; role grants; disposable PostgreSQL integration and Compose smoke gates                                    | No public DNS, production secret, persistent volume, rollback rehearsal, failover, capacity result, or deployment exists                                                                                                                                                     |
| Wrapped-key metadata and backup manifests | Per-record AES-GCM data keys wrapped by an injected AES-KW provider; provider/version binding; rewrap without ciphertext change; fail-closed missing/wrong-key tests; Ed25519-signed generation/parent manifests with ciphertext digest and external anti-rollback inventory                                                                                                       | The provider is an in-memory WebCrypto adapter. There is no external KMS/file custody, `pg_dump`, `pg_restore`, age encryption, retention destruction, or recovery-time claim                                                                                                |
| WebAuthn ceremonies                       | Pinned `@simplewebauthn/server`; exact RP/origin configuration; loopback-only one-time bootstrap; registration/authentication/reauthentication ceremony purposes; expiring one-time challenges and assertion proofs; required user presence and verification; durable counters and opaque application credential identifiers                                                       | No browser endpoint, cookie/CSRF surface, virtual-authenticator browser matrix, real authenticator, real person, or public origin is enabled                                                                                                                                 |
| Durable identity and recovery             | PostgreSQL Owner Identity repository, revision serialization, digest-only session persistence, command replay protection, atomic ceremony/proof consumption, independent recovery-proof digests, rate-limit lock, and recovery expiry                                                                                                                                              | Recovery completion remains synthetic and disconnected from any real channel. No real account, session cookie, recovery provider, or migration restore is enabled                                                                                                            |
| PostgreSQL Plan scheduler/outbox          | PostgreSQL Plan state, processed-command, append-only audit, and allowed durable work share one transaction; semantic conflict rolls the Plan decision back; scheduling advances one domain stage per command; `SKIP LOCKED`, database-time leases, monotonic fencing, bounded retry/dead letter, restore-safe denial, and an idempotent content-free synthetic sink are exercised | The runtime worker deliberately rejects `advance_plan_stage` until a canonical application adapter is wired. There is no notification provider and the schema contains no Guardian, Recipient, final-notice, Veto Window, Delivery Hold, Automatic Fallback, or Release task |
| Executable import pipeline                | Exact-byte libmagic classification, ClamAV `INSTREAM`, Pandoc sandbox-mode JSON conversion, node/link allowlists, size/time/output bounds, cleanup, benign Markdown, EICAR, mismatch, malformed output, and protocol framing tests; source and image versions are pinned                                                                                                           | The mandatory executable gate currently invokes file and Pandoc as host CI processes and ClamAV in a container. It is not rootless OCI isolation evidence, an adversarial corpus, archive handling, a real upload, or a sandbox-support claim                                |

## Verification boundary

`pnpm check` is the local deterministic gate. It includes format, lint, type, unit/contract, build, and documentation checks. The mandatory GitHub workflow adds:

- a fresh PostgreSQL 18 service and cross-process persistence/contention/restore-safe tests;
- the digest-pinned migrator/API/worker/PostgreSQL Compose topology;
- source-hash-verified file 5.48, Pandoc 3.10.2, and digest-pinned ClamAV 1.5.4 executable tests; and
- desktop and mobile WebKit acceptance for the existing synthetic PWA.

Passing that workflow verifies only the disposable fixtures at the tested commit. It does not pass sections 1–11 of the v1 release gates.

## Stop condition

The next work is the [Phase 3B closure six](../product/PHASE_3B_CLOSURE_SIX.md). Do not accept personal data, enroll a real identity, contact a real destination, expose the runtime publicly, add Guardian authority, implement any state beyond Concern, publish a tag, create a GitHub release, or call this v1.
