{
  "name": "vidha",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @vidha/web dev",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "test:postgres": "pnpm --filter @vidha/platform test:postgres",
    "test:topology": "pnpm --filter @vidha/platform test:topology",
    "test:backup": "./scripts/rehearse-backup.sh",
    "test:isolation": "./scripts/rehearse-import-isolation.sh",
    "test:webauthn": "./scripts/rehearse-webauthn.sh",
    "test:scanner": "pnpm --filter @vidha/documents test:scanner",
    "test:domain": "pnpm --filter @vidha/domain test",
    "test:e2e": "playwright test",
    "test:pwa-upgrade": "playwright test --config playwright.pwa-upgrade.config.ts",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:docs": "./scripts/check-docs.sh",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm check:docs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "@types/node": "26.2.0",
    "eslint": "10.8.1",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.4",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vitest": "4.1.11"
  }
}
